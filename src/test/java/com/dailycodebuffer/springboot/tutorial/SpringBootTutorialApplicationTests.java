package com.dailycodebuffer.springboot.tutorial;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class SpringBootTutorialApplicationTests {

	// Unit test is testing only single layer , if we are test controller layer then we need to mock the service layer
	// if we are test service layer then we need to mock the repository layer
	// we will start test to service layer
	// then repository layer and then with controller layer
	// we go to DepartmentService and Generate Test...






}
